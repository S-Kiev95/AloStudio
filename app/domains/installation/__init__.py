"""Installation-wide configuration (Chatwoot's ``installation_configs``).

Settings that belong to the *deployment* rather than to any one account:
the Meta app credentials, webhook verify tokens, branding. They used to
live only in ``.env.local``, which meant changing one required SSH access
to the server.
"""
